A mondrain style layout  using only HTML and CSS
This layout contains series of rectangles of different sizes having colors either of red yellow and blue using only css grid
The grid container (<ul>) holds a particular block width and all child elements (<li>) have customized grid span